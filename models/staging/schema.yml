version: 2

sources:
  - name: staging
    database: analytics-451817
    schema: trips_data_all
      # loaded_at_field: record_loaded_at
    tables:
      - name: green_trip_data
      - name: yellow_trip_data
         # freshness:
           # error_after: {count: 6, period: hour}
      - name: fhv_trip_data

